{
  "name": "fis3-postpackager-sw-register",
  "version": "1.0.5",
  "description": "service worker register plugin for fis3",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && babel src -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "pwa",
    "service",
    "worker",
    "fis3"
  ],
  "author": "chenqiushi",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "babel-core": "^6.26.0",
    "etpl": "^3.2.0"
  }
}
